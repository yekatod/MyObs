Простые типы — это псевдонимы для предопределенных `struct` типов в `System` пространстве имен, как описано в таблице ниже.

## Особенности:
- Можно создать литерал
- Только они имеют константы
- Константы вычисляются в момент компиляции