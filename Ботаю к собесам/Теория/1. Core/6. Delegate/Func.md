
---

Встроенный универсальный [[Delegate]] с параметром/параметрами и возвращает указанный тип. Может принимать до 12 параметров.

```C#
Func<string, string> selector = str => str.ToUpper();
```

Где `Func<input, return>` принимает сначала тип параметра, затем - тип возвращаемого значения